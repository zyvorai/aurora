[project]
name = "gtm-workers"
version = "0.1.0"
description = "AI Technical GTM Agent Platform Workers"
requires-python = ">=3.11"
dependencies = [
    "gtm-api",
    "arq>=0.26.0",
    "redis>=5.2.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.3.0",
    "httpx>=0.28.0",
    "structlog>=24.4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gtm_workers"]
