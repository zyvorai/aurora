Aurora {{ .Chart.AppVersion }} installed.

1. Ensure backing services (Postgres, Redis, Qdrant, Neo4j, MinIO, Ollama) are reachable
   via the env Secret values you passed.
2. Health check:
     curl -s http://{{ include "aurora.fullname" . }}-api:{{ .Values.api.service.port }}/health
3. Licensing: AGPL-3.0 open source; commercial ACL via sales@zyvor.dev
   See COMMERCIAL_LICENSE.md / docs/LICENSING.md in the source repo.
